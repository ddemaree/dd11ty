---
export interface Props {
  url: string | URL;
}

const { url } = Astro.props;

const res = await fetch(`https://publish.twitter.com/oembed?dnt=true&url=${url}`)
const data = await res.json();
---
<figure class="dd-embed dd-embed-tweet">
  <Fragment set:html={data.html} />
</figure>